-# encoding: utf-8

-#  Copyright (c) 2008-2017, Puzzle ITC GmbH. This file is part of
-#  Cryptopus and licensed under the Affero General Public License version 3 or later.
-#  See the COPYING file at the top-level directory or at
-#  https://github.com/puzzle/cryptopus.

%h1= t '.title'
%table.table.table-striped#teamlist_table
  %tr
    %th.teamlist-row= t '.teamname'
    %th.teamlist-row= t '.folders'
    %th.teamlist-row= t '.accounts'
    %th.teamlist-row= t '.members'
  - @teams.each do |team|
    - @team = team
    %tr{class: "#{cycle( 'odd', 'even' )}", 'team-id': team.id}
      - if team.private?
        %td= image_pack_tag( "encrypted_small.svg" ) + ' ' + team.name
      - else
        %td= image_pack_tag( "members.svg" ) + ' ' + team.name
      %td= number_of_folders
      %td= number_of_accounts
      %td
        =link_to('Show', '#', class: 'members-link')
