- if session[:user_id]
  %li#option.logout
    = link_to(logout_login_path) do
      %svg.icon-sign-out.sm
        %use{"xlink:href" => "#icon-sign-out"}
      = t(:logout)
    %small#countdownWrapper
      = t :auto_logout
      %span#countdown
