<div class="container py-2">
  {{#if @model}}
    {{#each @model as |team|}}
      <TeamShow @team={{team}} />
    {{/each}}
  {{else}}
    <div class="mt-4">
      <h5>{{t "teams.index.no_content"}}</h5>
    </div>
  {{/if}}
</div>
