-#  Copyright (c) 2008-2017, Puzzle ITC GmbH. This file is part of
-#  Cryptopus and licensed under the Affero General Public License version 3 or later.
-#  See the COPYING file at the top-level directory or at
-#  https://github.com/puzzle/cryptopus.

%h1= t '.title', group_name: @group.name, team_name: team.name
= link_to t('accounts.new.title'), account_path(@accounts), class: 'btn btn-primary pull-right'
%table.table.table-striped
  %tr
    %th{style: "width: 150px;"}= t '.account'
    %th= t :description
    %th{colspan: "3"}= t :actions
  - @accounts.each do |account|
    %tr{class: "#{cycle('odd', 'even')}"}
      %td= link_to image_tag( "key.svg" ) + " " + account.accountname, [account]
      %td= simple_format account.description
      %td.action_icon= link_to image_tag("edit.svg"), [:edit, account]
      %td.action_icon= link_to_destroy [account], account
