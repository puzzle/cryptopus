.login-form
  = form_tag(totp_path, class: 'login') do
    %fieldset
      = image_pack_tag "cryptopussy.svg", class: 'logo'
      .form-group
        = text_field_tag :totp_code, nil, id: 'totp_code', value: 'root', class: 'form-control', readonly: false
      = submit_tag t('.submit'), class: 'btn btn-primary'
